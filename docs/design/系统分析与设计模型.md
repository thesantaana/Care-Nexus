# 系统分析与设计模型

项目名称：CareNexus 颐联  
版本：轻量版 2.0  
更新时间：2026-07-15

## 1. 系统上下文

```mermaid
flowchart LR
    ADMIN[管理员] --> CN[CareNexus轻量版]
    CARE[护工] --> CN
    CN --> DB[(MySQL 8)]
    CN --> FS[(本地文件存储)]
    CN -.可选.-> AI[DeepSeek API]
```

系统只围绕培训管理和学习闭环，不连接护理订单、健康监测设备或医疗信息系统。

## 2. 包图

```plantuml
@startuml
package frontend {
  package portal_web
  package admin_web
  package mobile_web
}

package backend {
  package common
  package auth
  package training {
    package catalog
    package resource
    package exam
    package interaction
    package note
  }
  package ai
  package file
  package audit
}

frontend --> backend : REST / JSON
backend.auth --> backend.common
backend.training --> backend.auth
backend.training --> backend.file : Service
backend.ai --> backend.training : Resource source
backend.ai --> backend.auth
backend.audit --> backend.auth
backend --> database : MyBatis-Plus
@enduml
```

## 3. 核心领域类图

```mermaid
classDiagram
    SysRole "1" --> "*" SysUser : mainRole
    SysRole "*" --> "*" SysPermission : rolePermission

    TrainingCategory "1" --> "*" TrainingResource
    TrainingResource "*" --> "*" TrainingTag : resourceTag
    TrainingResource "1" --> "0..1" TrainingExam
    TrainingResource "1" --> "*" ExamQuestion
    TrainingExam "*" --> "*" ExamQuestion : examQuestion

    SysUser "1" --> "*" LearningAccessLog
    SysUser "1" --> "*" LearningRecord
    SysUser "1" --> "*" ExamRecord
    ExamRecord "1" --> "*" ExamAnswer

    SysUser "1" --> "*" TrainingNote
    TrainingResource "1" --> "*" TrainingNote

    TrainingResource "1" --> "*" TrainingDiscussion
    TrainingDiscussion "1" --> "*" TrainingDiscussionReply
    TrainingDiscussion "1" --> "*" TrainingDiscussionLike
    TrainingDiscussionReply "1" --> "*" TrainingDiscussionReplyLike

    TrainingResource "1" --> "*" TrainingAssignment
    TrainingAssignment "1" --> "*" TrainingAssignmentSubmission

    AiDraft "*" --> "*" TrainingResource : source
    AiDraft "0..1" --> "0..1" ExamQuestion : approvedCreates
```

## 4. 主要实体职责

| 实体 | 职责 |
|---|---|
| `SysUser` | 账号、主角色、状态和基础资料 |
| `TrainingResource` | 课程元数据、内容、存储方式和发布状态 |
| `LearningRecord` | 用户整体培训学习汇总 |
| `LearningAccessLog` | 每次课程访问时长 |
| `TrainingExam` | 课程对应考核与通过线 |
| `ExamQuestion` | 单选题或判断题 |
| `ExamRecord` | 一次考试尝试、分数和结果 |
| `TrainingNote` | 用户与课程唯一的富文本笔记 |
| `TrainingDiscussion` | 课程讨论主题 |
| `TrainingAssignment` | 课程课后作业 |
| `AiDraft` | AI 题目草稿及审核状态 |

## 5. 登录时序

```mermaid
sequenceDiagram
    participant U as 用户
    participant F as 前端
    participant A as AuthController
    participant S as AuthService
    participant D as MySQL

    U->>F: 输入账号密码
    F->>A: POST /api/v1/auth/login
    A->>S: login(request)
    S->>D: 查询用户、角色、权限
    S->>S: 校验状态与BCrypt密码
    S-->>A: LoginResponse
    A-->>F: Token + 用户 + 权限
    F-->>U: 按角色进入对应工作台
```

## 6. 课程发布时序

```mermaid
sequenceDiagram
    participant A as 管理员端
    participant C as TrainingResourceController
    participant S as TrainingResourceService
    participant D as MySQL
    participant L as OperationLogService

    A->>C: 创建资源
    C->>S: createResource
    S->>D: 保存DRAFT
    S-->>A: 资源详情
    A->>C: PUT /{id}/publish
    C->>S: publishResource
    S->>D: 检查状态并更新PUBLISHED
    S->>L: 记录发布操作
    S-->>A: 发布后资源
```

## 7. 考试提交时序

```mermaid
sequenceDiagram
    participant C as 护工端
    participant E as TrainingExamController
    participant T as TrainingExamTakingService
    participant S as TrainingScoreService
    participant D as MySQL

    C->>E: 获取已发布考核
    E->>T: getPublishedExam
    T->>D: 批量加载题目与选项
    T-->>C: 考核详情
    C->>E: 提交答案
    E->>T: submitExam
    T->>D: 校验用户、考核与尝试次数
    T->>T: 自动评分
    T->>D: 保存ExamRecord和ExamAnswer
    T->>S: 更新/计算课程成绩
    T-->>C: 分数、通过状态和解析
```

## 8. AI 草稿审核时序

```mermaid
sequenceDiagram
    participant A as 管理员端
    participant C as TrainingAiController
    participant S as AiQuestionDraftService
    participant R as TrainingAiSourceService
    participant M as AiTrainingService
    participant D as MySQL

    A->>C: 生成题目草稿
    C->>S: generate
    S->>R: 校验并加载TEXT资料
    R->>D: 查询资源与权限
    S->>M: generateQuestionDraft
    M-->>S: 草稿内容
    S->>D: 保存AiDraft和来源关系
    S-->>A: 草稿
    A->>C: 审核通过
    C->>S: review
    S->>D: 原子检查DRAFT状态
    S->>D: 创建DRAFT ExamQuestion
    S->>D: 更新APPROVED
    S-->>A: 审核结果
```

## 9. 笔记保存时序

```mermaid
sequenceDiagram
    participant U as 护工端
    participant C as TrainingNoteController
    participant S as TrainingNoteService
    participant D as MySQL

    U->>C: GET /notes/resource/{resourceId}
    C->>S: getByResource
    S->>D: 按当前用户+课程查询
    S-->>U: 笔记或空白结构
    U->>C: PUT 保存标题和HTML
    C->>S: save
    S->>D: 插入或更新唯一记录
    S-->>U: 保存后的笔记
```

## 10. 讨论回复模型

- 主题属于课程和创建用户。
- 回复属于主题和创建用户。
- `parent_reply_id` 支持回复另一条回复。
- 主题点赞与回复点赞分别使用唯一关系表，保证同一用户只点赞一次。
- 删除操作必须校验当前用户是内容作者。

## 11. 状态模型

### 培训资源

```mermaid
stateDiagram-v2
    [*] --> DRAFT
    DRAFT --> PUBLISHED: 发布
    PUBLISHED --> OFFLINE: 下架
    OFFLINE --> PUBLISHED: 重新发布
```

### 考核

```mermaid
stateDiagram-v2
    [*] --> DRAFT
    DRAFT --> PUBLISHED: 发布
```

### AI 草稿

```mermaid
stateDiagram-v2
    [*] --> DRAFT
    DRAFT --> APPROVED: 审核通过
    DRAFT --> REJECTED: 审核驳回
```

### 学习与培训

课程根据考试记录计算通过状态；整体培训根据全部已发布课程是否通过计算。前端不直接写入最终状态。

## 12. 设计约束

- 两角色边界不得被前端参数绕过。
- 模型不包含护理订单、老人家属和医生健康实体。
- AI 草稿与正式题目分离。
- 历史 36 表模型不适用于当前轻量版。
- 现行模型以 28 张表和当前 Java Entity 为准。