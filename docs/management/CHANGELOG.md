# Changelog

本文档记录项目过程和重要变更。不得记录未实际发生的功能完成情况。

## 2026-07-07

- 审计确认仓库已存在 `README.md`、`AGENTS.md` 和基础 `docs/` 说明文件。
- 新增过程管理目录和审计记录文件，已完成项目负责人审核；对应提交记录以 Git 历史为准。
- 新增需求澄清流程文件 `docs/workflows/REQUIREMENT_CLARIFICATION.md`，并在 `AGENTS.md` 中加入需求澄清入口规则，已完成项目负责人审核；对应提交记录以 Git 历史为准。
- 修正 AI 协作规则为按负责人和当前任务限制，不再全局禁止 AI 开发，已完成项目负责人审核；对应提交记录以 Git 历史为准。
- 统一文档目录体系，删除重复阶段目录，新增 `docs/STAGE_INDEX.md`，已完成项目负责人审核；对应提交记录以 Git 历史为准。
- 过程管理目录和需求澄清规范已由项目负责人确认通过；对应提交记录以 Git 历史为准。
- 确认当前仓库尚未包含后端、前端、数据库、部署或测试代码。
- 完成 T-003 当前阶段团队分工和模块负责人记录，AI 模块负责人确认为隋咏轩、孙洋。
- 新增并审核通过 `docs/plans/配置管理计划.md`，明确分支策略、提交规范、PR 评审、基线管理、标签规则、变更控制、交付归档和模块保护规则；T-004 当前记录为 `DONE` 状态。

## 2026-07-08

- 新增 `docs/plans/项目开发计划.md`，采用课程 12 天与正式项目第 7-12 天的双时间轴，明确项目范围、MVP边界、成员职责、阶段任务、里程碑、风险和验收标准。
- 新增 `docs/plans/项目进度计划.md`，明确先启、精化、构建、产品化四阶段进度、每日任务、任务依赖、里程碑和交付物检查表。
- T-005 项目开发计划和项目进度计划已经完成项目负责人审核，当前记录为 `DONE` 状态。
- 记录下一步进入 T-006 软件需求规约初稿；`develop` 分支仍在 T-009 需求基线确认后创建，当前尚未进入需求基线、架构设计、数据库设计和业务编码阶段。
- 新增并审核通过 `docs/requirements/软件需求规约.md`，覆盖护理培训系统、老人/家属移动护理平台、护工/护理人员服务执行、医生服务系统和综合管理系统的 MVP 需求、状态流转、AI 边界和需求追踪编号；T-006 当前记录为 `DONE` 状态。
- 修正 `docs/plans/项目进度计划.md` Mermaid 甘特图中 T-005 的状态，将项目开发计划和进度计划标记为 `done`。
- 根据项目负责人补充确认，细化 AI 出题规则为基于已上传或已入库培训资料生成题目草稿，草稿需培训管理员审核后才能进入正式题库或考核。
- 根据项目负责人审核意见，补充医生与老人授权关系需求，调整护理订单主状态、评价状态和投诉状态拆分规则，明确整体培训通过规则。
- 当前下一步进入 T-007 用例模型和业务流程初稿；`develop` 分支仍在 T-009 需求基线确认后创建，当前尚未进入架构设计、数据库设计和业务编码阶段。
- 新增并审核通过 `docs/requirements/用例模型.md`，基于已审核的软件需求规约建立参与者、系统边界、PlantUML 用例图、关键用例描述和需求追踪表；T-007 当前记录为 `DONE` 状态。
- 新增 `docs/requirements/业务流程.md`，使用 Mermaid 描述培训、AI辅助、预约订单、取消、评价投诉、医生授权、健康预警、随访干预评估和用户权限管理流程。
- 将 `docs/requirements/软件需求规约.md` 文档状态更新为“初稿，已审核，待 T-009 形成需求基线”。
- 根据项目负责人审核意见，拆分订单分配与执行用例，补充健康记录管理流程，调整 AI 辅助培训流程和医生老人授权参与者边界。
- 当前下一步进入 T-008 测试计划初稿；`develop` 分支仍在 T-009 需求基线确认后创建，当前尚未进入架构设计、数据库设计和业务编码阶段。
- 新增 `docs/test/测试计划.md`，形成 T-008 测试计划初稿，覆盖测试目标、测试范围、MVP优先级、测试类型、四端核心流程测试策略、角色权限、状态流转、AI测试边界、测试环境规划、测试数据规划、缺陷管理、进入退出条件和需求追踪方式。
- T-008 仅形成测试计划初稿，未编写详细测试用例，未执行软件测试，未创建前端、后端、数据库或部署工程。
- 当前下一步为审核 T-008 测试计划初稿，并在通过后进入 T-009 需求基线确认；`develop` 分支仍在 T-009 需求基线确认后创建。
- T-008 测试计划初稿已经完成项目负责人审核，当前记录为 `DONE` 状态；下一步进入 T-009 需求基线确认。
- 新增 `docs/management/需求基线评审记录.md` 草案，对软件需求规约、用例模型、业务流程和测试计划进行一致性、状态流转、角色权限、AI边界、MVP边界和追踪覆盖检查；草案阶段 T-009 记录为 `REVIEW` 状态。
- 修正 `docs/test/测试计划.md`，将 AI 模块变更边界从系统运行时权限测试调整为配置管理和过程审计检查，并收紧严重缺陷延期和退出条件。
- 草案阶段尚未正式形成需求基线，未创建 `develop` 分支，未创建阶段标签，未启动 T-010 或 T-011。
- T-009 需求基线评审已经通过最终确认，形成 CareNexus 需求基线 v1.0；软件需求规约、用例模型、业务流程和测试计划文档状态已更新为“需求基线 v1.0，已确认”。
- 当前允许进入精化阶段，启动 T-010 详细测试用例设计和 T-011 架构、数据库、接口及工程骨架准备；尚未开始业务功能编码。

## 2026-07-09

- 基于 CareNexus 需求基线 v1.0，从 `develop` 创建任务分支 `docs/T-010-detailed-test-cases`，用于 T-010 详细测试用例设计。
- 新增并审核通过 `docs/test/测试用例.md`，覆盖护理培训、AI辅助培训、移动护理、护工服务执行、医生服务、综合管理、权限安全和非功能检查，共 95 条详细测试用例。
- 新增 AI 固定验收数据表、角色权限测试矩阵、状态流转测试矩阵和 REQ-UC-TS-TC 追踪矩阵，所有测试用例当前执行状态均为“未执行”。
- 根据项目负责人审核意见，修正未确定用例为 T-011 设计依赖，补充密码、敏感配置和文件上传安全测试用例，拆分医生与健康管理人员权限边界。
- T-010 当前记录为 `DONE` 状态；本轮未执行软件测试，未创建前端、后端、数据库或部署工程，未编写业务代码。
- PR #1 已合并到 `develop`，随后从 `develop` 创建 `feature/T-011-architecture-skeleton` 执行 T-011。
- 新增 `docs/design/系统架构设计.md`、`docs/design/数据库设计.md`、`docs/api/API设计规范.md`、`docs/design/工程骨架说明.md` 和 `docs/decisions/ADR-001-总体架构与工程结构.md`。
- 新增后端 Spring Boot 工程骨架 `backend/`，包含统一响应、全局异常、基础安全配置、健康检查、文件存储接口、AI培训辅助适配接口、模块包结构和基础测试。
- 新增前端工程骨架 `frontend/admin-web/` 和 `frontend/mobile-web/`，采用 Vue 3 + Vite，分别承载 PC 工作台和移动端工作台基础结构。
- 新增数据库目录 `database/`，包含 MySQL 8 建表脚本、演示数据规划和数据字典说明；脚本执行验证因本机 MySQL 服务权限限制未完成。
- T-011 当前记录为 `REVIEW` 状态；本轮仅创建工程骨架和基础设施，不实现完整业务功能，不执行详细业务测试用例。
- 根据项目负责人“修改后复审”意见，补充 `docs/design/系统分析与设计模型.md` 和 `docs/design/原型设计.md`，包含包图、核心领域类图、关键时序图、部署图和低保真页面原型。
- 新增 `docs/api/MVP接口清单.md`，形成 MVP 接口设计清单初稿。
- 新增 `database/model/README.md`、`database/model/CDM说明.md`、`database/model/PDM说明.md`；未伪造 `.pdm` 或截图，PowerDesigner模型和截图后续按 T-030 / Issue #2 基于最终版 SQL 生成。
- 修正模块边界：`admin/auth` 只负责用户、角色、权限、账号和基础字典，订单归 `care`，医生授权和健康管理归 `doctor`，AI草稿审核归 `training`，操作日志归 `audit`。
- 修正数据库模型，实际表数量为 36 张，包含老人账号与老人档案关系、考核模型、题目选项、试卷题目关系、核心外键、受控手机号保存字段、培训资源存储模式和评价/投诉独立状态来源。
- 加固后端安全骨架：健康检查匿名访问，其他接口默认认证；补充 CORS、异常状态码映射、文件上传校验、Token接口、当前用户接口、PasswordEncoder 和 Checkstyle。
- 为两个前端工程补充 ESLint 和静态原型页面；T-011 仍保持 `REVIEW`，未创建标签，未合并到 `develop`，未开始完整业务编码。
- 根据最终复审意见，修正数据权限模块边界：`auth` 只负责当前用户、Token、RBAC 和账号状态，医生老人授权由 `doctor` 模块校验，家属老人绑定和护工订单范围由 `care` 模块校验。
- 补全 `docs/api/MVP接口清单.md` 至 96 个 MVP 接口，统一分页参数为 `pageNo/pageSize`，AI 题目草稿审核统一为 `review` 单接口。
- 统一数据库角色数据源：保留 `sys_user.main_role_id`，删除 `sys_user_role`；AI 草稿改为 `ai_draft_source_resource` 多资料来源关系。
- 加固安全和前端请求骨架：登录和健康检查匿名放行，其他接口默认认证；401/403 返回统一 JSON；CORS 使用 `ConfigurationProperties`；前端请求自动携带 Bearer Token 并处理 FormData 和 401。
- 使用临时 MySQL 8 实例重新执行 `001_schema.sql` 和 `002_seed_data.sql`，验证通过：36 张表、50 个外键、17 个唯一约束。
- T-011 架构、数据库设计、接口规范和工程骨架已完成项目负责人最终复审并封板；PowerDesigner模型和截图拆分为 T-030 / Issue #2，最终交付前基于最终版 SQL 生成，不阻塞当前业务开发。
- 当前下一步进入 T-012：账号登录、当前用户和 RBAC 权限基础。

## 2026-07-09 T-012 后端账号登录、当前用户与 RBAC 权限基础

- 从最新 `develop` 创建 `feature/T-012-auth-rbac`，启动 T-012 后端认证和 RBAC 基础实现。
- 新增后端登录、当前用户、退出登录、JWT 生成解析、Bearer Token 过滤器、账号状态校验、RBAC 权限加载和最小权限验证接口。
- 更新演示账号种子数据，统一演示密码为 `Demo@123456` 的 BCrypt 哈希；不保存明文密码。
- 新增 T-012 自动化测试，覆盖登录成功/失败、Token 缺失/错误/过期/黑名单、logout、RBAC 200/403、账号停用后旧 Token 失效和权限变更即时生效。
- `mvn verify` 已通过；本轮未修改前端目录，未重新执行前端构建。
- MySQL 8 真实联调已补跑通过：数据库脚本执行成功，后端启动成功，真实 HTTP 验证覆盖 login、me、RBAC 验证、logout、旧 Token 401、停用账号 401 和错误密码 401。
- T-012 已通过项目负责人审核，经 PR #4 合并到 `develop`，状态更新为 `DONE`。
- 合并后下一步进入 T-013：前端登录与权限接入；T-012 任务分支删除后不再继续使用。
- 补充 `docs/management/TASKS.md` 总任务路线图，新增 T-014 至 T-025 以及 T-030 高层任务，当前均为 `TODO`，未虚构业务功能完成情况。
- 新增 `docs/management/tasks/TASK-T-013-frontend-auth.md`，明确 T-013 前端登录与权限接入的负责人、建议分支、修改范围、实现范围、不实现范围和验收标准。
- 同步更新 `PROJECT_STATUS.md` 和 `docs/plans/项目进度计划.md`，将下一步明确为 T-013；本轮仅进行任务规划更新，未创建开发分支，未修改前后端业务代码。

## 2026-07-09 T-014 培训类别、标签和资源管理后端

- 从最新 `develop` 创建 `feature/T-014-training-resources`，启动 T-014，与 T-013 前端登录接入并行推进。
- 新增培训类别、培训标签和培训资源管理后端接口，覆盖列表、新增、修改、启停、分页、详情、发布和下架。
- 复用 T-012 当前用户、JWT 和 RBAC 权限基础，管理操作要求 `training:resource:manage`，查询操作允许 `training:resource:view` 或 `training:resource:manage`。
- 更新 `database/init/002_seed_data.sql`，为 `CAREGIVER` 补充 `training:resource:view` 查看权限。
- 新增 T-014 MockMvc 自动化测试 21 个；后端 `mvn test` 已通过，累计执行 44 个测试，Failures 0，Errors 0。
- 本轮未修改前端目录，未执行前端 lint/build；原因是 T-014 为后端独立任务。
- T-014 已通过项目负责人审核，经 PR #5 Squash and merge 合并到 `develop`，状态更新为 `DONE`。

## 2026-07-10 T-016 题库、考核和学习记录后端

- 从最新 `develop` 创建 `feature/T-016-exam-learning-records`，启动 T-016。
- 根据用户确认，考核题型调整为仅支持单选题和判断题，不实现简答题、人工评分或待评分状态。
- 新增题库、题目选项、考核、考核题目关联、学习访问记录、我的学习记录、获取考核和提交考核后端接口。
- 更新数据库脚本，为 `exam_question` 增加 `resource_id`，并将演示种子数据替换为 3 条护工培训资源和 9 道客观题。
- 新增 T-016 自动化测试并补充判断题无选项组装回归测试；后端 `mvn verify` 已通过，累计执行 52 个测试，Failures 0，Errors 0，Checkstyle 0 violations。
- 本轮未修改前端目录，未执行前端 lint/build；原因是 T-016 为后端独立任务。
- 使用 MySQL 8.0.45 隔离实例完成真实脚本导入和最小 HTTP 联调，验证护工登录、学习门槛、考核获取、客观题自动评分和培训通过状态；同时修复判断题无选项时的 NPE 和只读登录事务中操作日志无法写入的问题。
- T-016 后端功能实现和自动化测试已通过项目负责人审核，状态更新为 `DONE`；MySQL 正式环境导入、完整真实 HTTP 联调及跨模块回归测试统一纳入 T-023。
## 2026-07-10 后端复杂度和模块边界整理

- 按项目负责人要求，对 T-014 培训资源管理后端和 T-016 题库、考核、学习记录后端进行可读性和复杂度整理。
- 删除无业务用途的空模块标记类，拆除 `TrainingStatuses` 大杂烩常量，按资源状态、考核状态、考核结果、学习状态和题型分别维护常量。
- 将原培训资源大 Service 拆分为类别标签、资源写入、资源查询、资源标签批量加载和访问权限等职责；training 模块通过 `FileResourceService` 读取文件资源，不再直接依赖 `FileResourceMapper`。
- 将题库、考核、学习记录和考试提交拆分为管理、题目、学习、答题、组装和公共支撑服务，并将考核详情中的题目和选项加载调整为批量查询。
- 补充 `AGENTS.md` 和工程骨架说明中的后端复杂度控制规则，后续开发继续按模块边界、Service 规模、批量查询和跨模块 Service 协作执行。
- 后端 `mvn verify` 已通过，累计执行 52 个测试，Failures 0，Errors 0，Checkstyle 0 violations。
- MySQL 8.0.45 真实导入和 T-016 最小 HTTP 联调已补跑通过；本机 3306 服务曾发生 Windows I/O 崩溃，因此验证使用同版本隔离实例的 3307 端口完成。

## 2026-07-10 T-018 护理预约与订单闭环后端

- T-018 已完成需求确认，从最新 `develop` 创建 `feature/T-018-care-order` 开始实现。
- 确认范围包括家属绑定、服务项目、地址、预约、人工分配、护工执行、取消、评价和投诉后端闭环。
- 已完成护理领域实体、Mapper、DTO/VO、数据权限、业务服务和 REST 接口，增加订单状态原子更新以及评价、投诉重复操作保护。
- 联系电话改为使用环境配置驱动的可恢复密文保存，对外响应仍只展示脱敏尾号，不在日志中记录完整号码。
- 更新数据库脚本和数据字典，增加绑定码哈希、护理权限种子、投诉唯一约束及必要外键；当前脚本设计为 36 张表、53 个外键和 18 个唯一约束。
- 后端 `mvn verify` 已通过，累计执行 59 个测试，Failures 0，Errors 0，Checkstyle 0 violations；T-018 状态更新为 `REVIEW`。
- 正式 MySQL 导入、完整真实 HTTP 联调和跨模块回归测试统一纳入 T-023。

## 2026-07-11 T-022 AI培训辅助后端

- T-018 和 T-020 已分别通过 PR #6、PR #7 合并，过程状态同步为 DONE。
- 新增培训资料问答、总结、本人学习建议、题目草稿生成、草稿分页、草稿详情和草稿审核 7 个稳定 Mock 接口。
- AI输入限制为调用人有权访问的 TEXT 培训资源，普通用户只能访问已发布资源，响应保留资源引用且不读取医生健康数据。
- 题目草稿仅支持单选题和判断题；审核通过创建 DRAFT 正式题目但不自动发布或加入考核，驳回不创建题目，重复审核返回冲突错误。
- 后端 `mvn verify` 已通过，共执行 72 个测试，Failures 0，Errors 0，Checkstyle 0 violations。
- T-022 状态更新为 REVIEW，等待项目负责人审核；真实模型、MySQL 完整 HTTP 联调、跨模块回归和前端页面不在本任务范围内。

## 2026-07-11 T-013 / T-015 前端联调实现

- 在 `feature/frontend-integration` 实现 PC、移动端的登录、会话恢复、Bearer Token、401 清理、退出登录、路由守卫和权限菜单。
- 新增 PC 培训资源、类别和标签管理，以及移动端已发布资源浏览、学习访问和学习进度页面；均调用现有认证或 training 后端接口，不使用业务 Mock 数据。
- 订单、医生和综合管理接口尚未实现，前端保留权限受控的待接入页面，不伪造业务记录。
- 两个前端 lint/build 与 Vite 入口 HTTP 冒烟检查通过；真实 HTTP 联调因本机后端环境不可用而待执行。
- 将完整 React 门户前端及图片、视频静态资源纳入 `frontend/portal-web`，生产构建通过。
