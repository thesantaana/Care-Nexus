# 2026-07-09 隋咏轩工作日志

## 当前阶段

精化阶段：T-011 系统架构、数据库设计、接口规范和工程骨架准备。

## 任务编号

T-011：架构、数据库设计和工程骨架准备。

## 今日目标

- 基于 CareNexus 需求基线 v1.0 和 T-010 详细测试用例，完成系统总体架构确认。
- 创建架构设计、数据库设计、API 设计规范、工程骨架说明和 ADR。
- 创建可运行但不包含完整业务功能的后端、前端和数据库工程骨架。
- 验证工程骨架可以构建、启动和进行基础健康检查。

## 实际完成内容

- 合并 T-010 PR 后，从 `develop` 创建 `feature/T-011-architecture-skeleton` 分支。
- 确认 T-011 采用 Java 8、Spring Boot 2.7.x、模块化单体、PC 管理端和移动端两个前端工程。
- 明确 PC 端和移动端都可以承载护理培训等业务入口，逻辑上继续保留护理培训、移动护理、医生服务和综合管理四端边界。
- 新增系统架构设计、数据库设计、API 设计规范、工程骨架说明和 ADR。
- 新增后端 Spring Boot 工程骨架、前端 PC 管理端骨架、前端移动端骨架和数据库初始化目录。
- 登记实际创建的 AI 适配层目录为受保护范围。
- 将 T-011 状态更新为 `REVIEW`，等待项目负责人审核。

## 修改文件

- `docs/design/系统架构设计.md`
- `docs/design/数据库设计.md`
- `docs/design/工程骨架说明.md`
- `docs/api/API设计规范.md`
- `docs/decisions/ADR-001-总体架构与工程结构.md`
- `backend/`
- `frontend/admin-web/`
- `frontend/mobile-web/`
- `database/`
- `docs/management/TASKS.md`
- `docs/management/PROJECT_STATUS.md`
- `docs/management/CHANGELOG.md`
- `docs/management/MODULE_OWNERSHIP.md`
- `docs/plans/项目进度计划.md`
- `docs/test/TEST_LOG.md`

## 实际执行的检查

- `java -version`
- `mvn -version`
- `node -v`
- `npm -v`
- `git --version`
- `mysql --version`
- `docker --version`
- `Get-Service` 检查 MySQL 服务状态
- `Start-Service MySQL80`
- `Start-Service mysql`
- `mvn test`
- `mvn package`
- `npm install`
- `npm run build`
- `npm audit --omit=dev`
- 后端启动后访问 `GET /api/v1/health`

## 测试和构建结果

- 后端 `mvn test` 通过，执行 1 个 Spring Boot 上下文加载测试。
- 后端 `mvn package` 通过，成功生成 Spring Boot jar。
- 后端健康检查通过，`GET /api/v1/health` 返回 HTTP 200 和 `status=UP`。
- PC 管理端 `npm run build` 通过。
- 移动端 `npm run build` 通过。
- PC 管理端和移动端 `npm audit --omit=dev` 均显示生产依赖 0 vulnerabilities。
- MySQL 客户端存在，但本机 MySQL 服务因当前终端权限不足无法启动，SQL 脚本未完成真实连接执行验证。
- Docker 当前未安装；T-011 仅将 Docker 作为后续增强规划。

## 遇到的问题

- 前端初次构建失败，原因是缺少 `vite.config.js` 中的 Vue 插件配置。
- Vite 和 Vue 插件初始放在生产依赖中，导致生产依赖审计误报开发服务器相关漏洞。
- MySQL 服务存在但处于停止状态，当前终端权限不足，无法启动服务。
- Docker 未安装。

## 解决方式

- 为 `frontend/admin-web` 和 `frontend/mobile-web` 补充 `vite.config.js`。
- 将 Vite 和 Vue 插件移动到 `devDependencies`，并重新执行 `npm install`。
- 在 `docs/test/TEST_LOG.md` 中如实记录 MySQL 和 Docker 环境限制。
- 未将未执行的 SQL 验证或业务测试写成通过。

## 未完成事项

- T-011 仍待项目负责人审核，未标记为 `DONE`。
- 尚未建立架构基线。
- 尚未实现完整业务功能。
- 尚未执行 `docs/test/测试用例.md` 中的详细业务测试用例。
- 数据库脚本尚未在本机 MySQL 服务中完成真实执行验证。

## 下一步计划

- 等待项目负责人审核 T-011 设计文档和工程骨架。
- 根据审核意见修正架构、数据库、API 或工程骨架。
- 审核通过后再进入架构基线或后续业务功能编码。

## 对应Git提交

对应提交记录以Git历史为准。

## T-011 修改后复审整改记录

### 当前阶段

精化阶段：T-011 修改后复审。

### 实际完成内容

- 根据项目负责人审核意见，保持 T-011 为 `REVIEW`，未改为 `DONE`。
- 新增 `docs/design/系统分析与设计模型.md`，补充后端包图、核心领域类图、关键时序图和部署图。
- 新增 `docs/design/原型设计.md`，补充 PC 首页、培训资源一级页面、培训资源详情页面、移动端服务首页、服务预约页面、护工订单执行页面和医生健康档案页面的低保真原型。
- 新增 `docs/api/MVP接口清单.md`，整理 56 个 MVP 接口。
- 新增 `database/model/README.md`、`database/model/CDM说明.md`、`database/model/PDM说明.md`。
- 修正模块边界，明确 `admin/auth` 不直接拥有所有业务表，业务模块通过 Service 接口协作。
- 修正数据库脚本，实际表数量调整为 36 张，并补充老人账号关联、考核模型、核心外键、唯一约束、培训资源存储模式、受控手机号保存字段。
- 加固后端安全、异常处理、文件存储校验、认证扩展接口和 Checkstyle。
- 尝试并完成 MySQL 真实执行验证：使用临时 MySQL 8 实例执行建表和演示数据脚本。

### 实际执行命令

- `mvn verify`
- `npm install`
- `npm run lint`
- `npm run build`
- `npm audit --omit=dev`
- 临时 MySQL 8 实例执行 `database/init/001_schema.sql` 和 `database/init/002_seed_data.sql`
- 后端启动后访问 `GET /api/v1/health`

### 验证结果

- 后端 `mvn verify` 通过，Checkstyle 0 violations。
- PC 管理端和移动端 `npm run lint` 均通过，无错误、无警告。
- PC 管理端和移动端 `npm run build` 均通过。
- 前端生产依赖审计 0 vulnerabilities。
- MySQL 真实执行验证通过：36 张表、50 个外键、17 个唯一约束。
- 后端健康检查返回 HTTP 200。

### 未完成事项

- T-011 尚未复审通过，不能标记为 `DONE`。
- 未创建 `elaboration-v1.0` 标签。
- 未合并到 `develop`。
- 未开始完整业务功能编码。
- 当前本机未检测到 PowerDesigner，尚未生成可打开的 `.pdm` 模型和 CDM/PDM 截图。

### 下一步计划

- 等待项目负责人对 T-011 修改后产出进行复审。
- 复审通过后再按配置管理流程提交和后续建立架构基线。

## T-011 最终修正记录

### 当前阶段

精化阶段：T-011 继续保持 `REVIEW`，等待项目负责人再次审核。

### 实际完成内容

- 修正数据权限模块边界，删除 `auth` 模块中的跨领域 `DataPermissionService`。
- 新增 `DoctorAuthorizationService`、`FamilyElderAccessService` 和 `CareOrderAccessService` 接口骨架，明确医生老人授权、家属老人绑定和护工本人订单范围由业务模块 Service 层校验。
- 补全 MVP 接口清单至 96 个接口，统一分页参数为 `pageNo/pageSize`，AI 草稿审核统一为 `review` 单接口。
- 删除数据库中的 `sys_user_role` 表，保留 `sys_user.main_role_id` 作为 MVP 阶段主要业务角色唯一数据源。
- 新增 `ai_draft_source_resource`，将 AI 草稿来源调整为多培训资料关系。
- 加固 Spring Security 默认策略、JSON 401/403 返回、CORS 配置、前端请求封装和文件上传 MIME/扩展名对应校验。
- 补充 Checkstyle 轻量规则并修正规则过严导致的连续字段空行问题。

### 实际执行命令

- `mvn verify`
- `npm run lint`
- `npm run build`
- 临时 MySQL 8 实例执行 `database/init/001_schema.sql` 和 `database/init/002_seed_data.sql`

### 验证结果

- 后端 `mvn verify` 通过，包含 Spring Boot 上下文测试、打包和 Checkstyle。
- PC 管理端 `npm run lint`、`npm run build` 通过。
- 移动端 `npm run lint`、`npm run build` 通过。
- MySQL 真实执行验证通过：36 张表、50 个外键、17 个唯一约束。

### 未完成事项

- T-011 已由项目负责人确认可封板，状态更新为 `DONE`。
- 未创建 `elaboration-v1.0` 标签。
- 未合并到 `develop`。
- T-012 尚未开始业务编码，需先提交并合并 T-011 到 `develop`。
- PowerDesigner 模型和截图已拆分为 T-030 / Issue #2，最终交付前基于最终版 SQL 人工生成。

## T-011 封板记录

- 项目负责人确认 T-011 拆分后可以封板。
- PowerDesigner最终成果已进入 GitHub Issue #2 的 T-030，最终产出为 `database/model/CareNexus.cdm`、`database/model/CareNexus.pdm`、`database/model/CareNexus-CDM.png`、`database/model/CareNexus-PDM.png`。
- T-030 标记为“最终交付前完成，不阻塞当前业务开发”。
- 本机环境检查确认 MySQL80 已启动，后端 `mvn verify` 通过，PC端和移动端 `npm run lint`、`npm run build` 均通过。
- 下一步提交并推送 T-011 分支，创建 PR 合并到 `develop`，再从最新 `develop` 创建 `feature/T-012-auth-rbac`。
## T-012 后端账号登录、当前用户与 RBAC 权限基础

### 当前阶段

构建阶段：T-012 已进入 `REVIEW`，当前完成后端实现、自动化测试和 MySQL 真实联调，等待项目负责人审核。

### 实际完成内容

- 从最新 `develop` 创建并使用 `feature/T-012-auth-rbac` 分支。
- 将 `docs/management/TASKS.md` 中 T-012 从 `TODO` 更新为 `DOING`，新增 T-013 前端登录与权限接入任务占位。
- 新增后端登录接口、当前用户接口、退出登录接口和 RBAC 验证接口。
- 实现 JWT 生成、解析、签名校验、过期校验和单机内存黑名单。
- 实现 Bearer Token 认证过滤器，将认证用户和权限码写入 `SecurityContext`。
- 实现每次请求重新加载账号状态、主角色和 RBAC 权限。
- 实现 BCrypt 密码校验，登录失败使用泛化提示。
- 更新演示账号种子数据，统一演示密码为 `Demo@123456` 的 BCrypt 哈希。
- 新增 MockMvc 自动化测试，覆盖登录、me、logout、Token异常、RBAC 200/403、账号停用和权限变更等场景。
- 新增 `docs/design/认证设计说明.md` 和 T-012 任务文档。

### 修改文件

- `backend/pom.xml`
- `backend/src/main/resources/application.yml`
- `backend/src/main/java/com/carenexus/auth/`
- `backend/src/main/java/com/carenexus/audit/`
- `backend/src/main/java/com/carenexus/common/config/SecurityConfig.java`
- `backend/src/main/java/com/carenexus/common/error/ErrorCode.java`
- `backend/src/main/java/com/carenexus/common/exception/GlobalExceptionHandler.java`
- `backend/src/test/java/com/carenexus/auth/AuthControllerIntegrationTest.java`
- `database/init/002_seed_data.sql`
- `database/dict/data-dictionary.md`
- `docs/api/MVP接口清单.md`
- `docs/api/API设计规范.md`
- `docs/design/认证设计说明.md`
- `docs/management/tasks/TASK-T-012-auth-rbac.md`
- `docs/management/TASKS.md`
- `docs/management/PROJECT_STATUS.md`
- `docs/management/CHANGELOG.md`
- `docs/plans/项目进度计划.md`
- `docs/test/TEST_LOG.md`
- `docs/logs/2026-07-09-隋咏轩.md`

### 实际执行命令

- `git branch --show-current`
- `git status --short`
- `git log -1 --oneline`
- `git merge-base --is-ancestor 23f494e HEAD`
- `mvn verify`
- `Get-Service -Name MySQL80,mysql`
- `Start-Service -Name MySQL80`

### 验证结果

- 当前分支确认是 `feature/T-012-auth-rbac`。
- 工作区开工前干净，当前分支基于 T-011 合并后的 `develop` 提交。
- 后端 `mvn verify` 通过，执行 23 个测试，Failures 0，Errors 0，Checkstyle 0 violations。
- 本轮未修改前端目录，未重新执行前端 lint 和 build；原因是 T-012 为后端独立任务。
- MySQL80 服务当前显示停止，当前终端执行 `Start-Service -Name MySQL80` 失败，真实 MySQL 联调尚未完成。
- 用户启动 MySQL80 服务后，已补跑真实 MySQL 联调：建库建表、种子数据、后端启动、login、me、RBAC验证、logout、旧Token 401、停用账号 401 和错误密码 401 均通过。

### 遇到的问题和解决方式

- 初次 `mvn verify` 中方法级 `@PreAuthorize` 抛出的 `AccessDeniedException` 被全局异常处理为 HTTP 500。已补充 `AccessDeniedException` 处理，统一返回 HTTP 403 和 `ApiResponse`。
- MySQL80 服务最初无法由当前终端启动。用户启动服务并提供本机连接信息后，已完成补跑联调。

### 未完成事项

- T-012 尚未提交、推送或创建 PR。
- 未提交、未推送、未创建 PR。

### 下一步计划

- 等待项目负责人审核 T-012。
- 审核通过后提交并推送 `feature/T-012-auth-rbac`，创建 PR 合并到 `develop`。

### 对应Git提交

对应提交记录以 Git 历史为准；本轮尚未提交。
