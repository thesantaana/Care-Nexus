# 2026-07-09 孙洋工作日志

## 当前阶段

精化阶段：T-011 前端工程骨架和 AI 培训辅助接入边界准备。

## 任务编号

T-011：架构、数据库设计和工程骨架准备。

## 今日目标

- 明确前端工程拆分方式和四端在 PC/移动端中的承载关系。
- 建立 PC 管理端和移动端可启动骨架。
- 配合确认 AI 模块只作为护理培训辅助能力，并通过独立适配层接入。

## 实际完成内容

本日志记录 T-011 中与孙洋负责范围相关的产出，需本人后续复核。

- 创建 `frontend/admin-web/`，作为 PC 管理端基础工程骨架。
- 创建 `frontend/mobile-web/`，作为老人、家属和护工移动端基础工程骨架。
- 两个前端工程均包含基础路由、登录占位、角色菜单结构、请求封装、统一错误处理入口和环境变量样例。
- 在架构设计中明确 PC 端和移动端都可承载护理培训入口，逻辑上继续保持四个业务端边界。
- 创建 `backend/src/main/java/com/carenexus/ai/` AI 培训辅助适配接口和 Mock 实现骨架。
- 明确 AI 只能基于入库培训资料提供问答、总结、学习建议和题目草稿生成；题目草稿必须经培训管理员审核后才能进入正式题库。

## 修改文件

- `frontend/admin-web/`
- `frontend/mobile-web/`
- `backend/src/main/java/com/carenexus/ai/`
- `docs/design/系统架构设计.md`
- `docs/design/工程骨架说明.md`
- `docs/management/MODULE_OWNERSHIP.md`

## 实际执行的检查

- `npm install`
- `npm run build`
- `npm audit --omit=dev`

## 测试和构建结果

- PC 管理端生产构建通过。
- 移动端生产构建通过。
- 两个前端工程生产依赖审计结果均为 0 vulnerabilities。
- 本轮没有执行详细业务功能测试，因为前端仅为工程骨架和占位页面。

## 遇到的问题

- 初次前端构建失败，缺少 Vue 插件配置。
- 前端开发工具依赖初始放入生产依赖，导致生产依赖安全审计不准确。

## 解决方式

- 增加 `vite.config.js` 并启用 `@vitejs/plugin-vue`。
- 将 Vite 和 Vue 插件调整到 `devDependencies`。

## 未完成事项

- 前端业务页面尚未实现。
- 真实 AI 模型尚未接入。
- AI 题目草稿审核发布流程尚未实现。
- 需孙洋本人后续复核前端骨架和 AI 接入边界。

## 下一步计划

- 等待项目负责人审核 T-011。
- 审核通过后，后续根据任务拆分实现具体页面和接口联调。

## 对应Git提交

对应提交记录以Git历史为准。

## T-011 修改后复审相关记录

### 当前阶段

精化阶段：前端原型和 AI 接入边界补充。

### 实际完成内容

本日志记录 T-011 修改后复审中与孙洋负责范围相关的产出，需本人后续复核。

- 补充 PC 管理端和移动端静态原型页面。
- PC 管理端增加首页、培训资源一级页面、培训资源详情页面、医生健康档案页面和综合管理入口。
- 移动端增加服务首页、服务详情与预约页面、护工订单执行页面和移动培训学习入口。
- 为两个前端工程增加 ESLint 配置和 `npm run lint`。
- 明确 AI 仍只作为护理培训辅助能力，通过独立适配层接入，真实模型接入和完整审核发布流程留待后续任务。

### 实际执行命令

- `npm install`
- `npm run lint`
- `npm run build`
- `npm audit --omit=dev`

### 验证结果

- PC 管理端 lint 通过，无错误、无警告。
- 移动端 lint 通过，无错误、无警告。
- 两个前端工程生产构建通过。
- 两个前端工程生产依赖审计 0 vulnerabilities。

### 未完成事项

- 前端仍为静态原型和路由骨架，未实现完整业务页面和接口联调。
- AI 真实模型、AI 草稿审核发布完整流程尚未实现。
- T-011 已由项目负责人确认可封板，下一步进入 T-012 登录和权限基础。

## T-011 封板记录

- PC端和移动端工程骨架、请求封装、静态原型和 AI 接入边界已随 T-011 通过复审。
- PowerDesigner成果不由前端负责，已拆分为 T-030 / Issue #2。
- 下一步配合 T-012 完成登录态、当前用户、RBAC菜单和接口鉴权基础。

## T-011 最终修正相关记录

### 实际完成内容

- 修正两个前端工程的 `request.js`，自动携带 Bearer Token。
- HTTP 401 时清理本地登录状态。
- FormData 请求不强制设置 `application/json`。
- 非 JSON 响应增加安全处理。
- AI 请求对象调整为 `sourceResourceIds`，与多资料来源设计保持一致。

### 实际执行命令

- `npm run lint`
- `npm run build`

### 验证结果

- PC 管理端 lint 和生产构建通过。
- 移动端 lint 和生产构建通过。
- 前端仍为请求封装和原型骨架，不代表业务接口已联调完成。
