# 2026-07-10 隋咏轩工作日志

## T-016 题库、考核和学习记录后端

### 当前阶段

构建阶段：T-016 已形成后端实现结果，当前状态为 `REVIEW`，等待项目负责人审核和 MySQL 真实联调补跑。

### 实际完成内容

- 从最新 `develop` 创建并使用 `feature/T-016-exam-learning-records` 分支。
- 阅读 T-016 相关过程规范、接口清单和 `caregiver_training_mvp.md` 培训资料。
- 按用户确认删除简答题范围，只保留单选题和判断题。
- 新增题库、题目选项、考核、考核题目关联、学习访问记录、我的学习记录、获取考核和提交考核后端能力。
- 实现客观题自动评分，提交后立即判定已通过或未通过。
- 实现整体学习时长记录、最近学习时间记录和考试准入检查。
- 将演示培训资料整理为 3 条培训资源和 9 道客观题种子数据。
- 同步清理需求、用例、流程、测试计划、测试用例、数据库设计、接口清单中的简答题、人工评分和待评分表述。

### 修改文件

- `backend/src/main/java/com/carenexus/training/`
- `backend/src/test/java/com/carenexus/training/TrainingExamControllerIntegrationTest.java`
- `backend/src/test/java/com/carenexus/auth/AuthControllerIntegrationTest.java`
- `backend/src/test/java/com/carenexus/training/TrainingResourceControllerIntegrationTest.java`
- `database/init/001_schema.sql`
- `database/init/002_seed_data.sql`
- `database/dict/data-dictionary.md`
- `docs/api/MVP接口清单.md`
- `docs/requirements/软件需求规约.md`
- `docs/requirements/用例模型.md`
- `docs/requirements/业务流程.md`
- `docs/test/测试计划.md`
- `docs/test/测试用例.md`
- `docs/design/系统分析与设计模型.md`
- `docs/design/数据库设计.md`
- `docs/management/TASKS.md`
- `docs/management/PROJECT_STATUS.md`
- `docs/management/CHANGELOG.md`
- `docs/management/tasks/TASK-T-016-exam-learning-records.md`
- `docs/plans/项目进度计划.md`
- `docs/test/TEST_LOG.md`

### 实际执行命令

- `git fetch origin`
- `git checkout develop`
- `git pull --ff-only origin develop`
- `git checkout -b feature/T-016-exam-learning-records`
- `cd backend; mvn test`
- `cd backend; mvn verify`
- `mysql -uroot -p*** ...`
- `Get-Service`
- `Start-Service -Name MySQL80`
- `Start-Service -Name mysql`

### 验证结果

- 后端 `mvn test` 通过，累计执行 51 个测试，Failures 0，Errors 0。
- 后端 `mvn verify` 通过，累计执行 51 个测试，生成 jar，Checkstyle 0 violations。
- 前端 lint/build 未执行，原因是 T-016 未修改 `frontend/admin-web` 和 `frontend/mobile-web`。
- MySQL 真实执行未完成，原因是本机 MySQL 服务当前停止，当前终端无权启动 `MySQL80` 或 `mysql` 服务。

### 遇到的问题和解决方式

- 初次 `mvn test` 中，学习门槛测试未 mock 已发布考核，导致先返回 404。已补充考核 mock 后重跑通过。
- 项目既有文档仍包含简答题、人工评分和待评分状态。已按用户确认同步删除或改写为客观题自动评分。
- MySQL 服务无法由当前终端启动。已记录为待补跑事项，不写成通过。

### 未完成事项

- T-016 尚未提交、推送或创建 PR。
- MySQL 真实数据库执行和 HTTP 联调待用户启动 MySQL 服务后补跑。
- T-017 前端页面未开始，属于后续任务。

### 下一步计划

- 等待项目负责人审核 T-016。
- 用户启动 MySQL 服务后补跑数据库脚本和真实 HTTP 联调。
- 审核通过后提交并创建 PR 到 `develop`。

### 对应Git提交

对应提交记录以 Git 历史为准。
## T-014/T-016 后端复杂度整理

### 当前阶段

构建阶段：在 T-014 和 T-016 后端功能已有实现基础上，按项目负责人提出的可读性、复杂度和模块边界规则进行整理。

### 实际完成内容

- 将培训类别和标签管理拆入 `training.catalog`。
- 将培训资源写入、查询、标签批量加载和权限判断拆入 `training.resource` 子包。
- 新增 `file.FileResourceService`，training 模块不再直接访问 `FileResourceMapper`。
- 将题库、考核、学习记录和考试提交拆分为管理、题目、学习、答题、组装和公共支撑服务。
- 将考核详情中的题目和选项加载改为批量查询，避免后续题目数量增加时产生明显 N+1 查询。
- 删除无业务用途的空模块标记类。
- 删除 `TrainingStatuses`，改为按业务概念拆分资源状态、考核状态、考核结果、学习状态和题型常量。
- 补充 `AGENTS.md` 和 `docs/design/工程骨架说明.md` 中的后端复杂度控制规则。

### 实际执行命令

- `cd backend; mvn test`
- `cd backend; mvn verify`
- `rg TrainingStatuses`
- `rg FileResourceMapper backend/src/main/java/com/carenexus/training`
- `Get-Service -Name MySQL80,mysql`
- `Start-Service -Name MySQL80`
- `Start-Service -Name mysql`
- `Get-NetTCPConnection -LocalPort 3306`

### 验证结果

- 后端 `mvn test` 通过，累计执行 51 个测试，Failures 0，Errors 0。
- 后端 `mvn verify` 通过，累计执行 51 个测试，生成 jar，Checkstyle 0 violations。
- training 主代码中未发现 `FileResourceMapper` 直接依赖。
- 主代码和测试中未发现 `TrainingStatuses` 引用。
- MySQL 真实导入补跑未完成。原因：`mysql` 和 `MySQL80` 服务均为 Stopped，当前终端启动服务失败，3306 端口未监听。

### 未完成事项

- MySQL 真实导入和 HTTP 联调仍需在 MySQL 服务实际启动后补跑。
- 前端 lint/build 未执行，原因是本轮未修改前端代码。

### 对应Git提交

对应提交记录以 Git 历史为准。
